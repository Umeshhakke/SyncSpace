// ==========================================================
// CodeBoard Design System
// Component Variants
// ==========================================================

export const buttonVariants = {
    primary:
        "bg-blue-600 text-white hover:bg-blue-700 focus:ring-2 focus:ring-blue-200",

    secondary:
        "bg-white border border-slate-300 text-slate-700 hover:bg-slate-100 focus:ring-2 focus:ring-slate-200",

    ghost:
        "bg-transparent text-slate-700 hover:bg-slate-100",

    danger:
        "bg-red-600 text-white hover:bg-red-700 focus:ring-2 focus:ring-red-200",
};

export const badgeVariants = {
    default: "bg-slate-100 text-slate-700",

    primary: "bg-blue-100 text-blue-700",

    success: "bg-green-100 text-green-700",

    warning: "bg-yellow-100 text-yellow-700",

    danger: "bg-red-100 text-red-700",

    dark: "bg-slate-800 text-white",
};

export const inputVariants = {
    default:
        "border-slate-300 focus:border-blue-500 focus:ring-2 focus:ring-blue-100",

    error:
        "border-red-500 focus:border-red-500 focus:ring-2 focus:ring-red-100",

    disabled:
        "bg-slate-100 opacity-70 cursor-not-allowed",
};

export const cardVariants = {
    default:
        "bg-white border border-slate-200",

    elevated:
        "bg-white border border-slate-200 shadow-lg",

    outline:
        "bg-transparent border border-slate-300",
};